<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.expense_manager">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:name=".Main"
        android:allowBackup="true"
        android:icon="@mipmap/expense"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.LOGIN">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.AddIncome" />
        <activity android:name=".Activities.AddExpense" />
        <activity android:name=".Activities.Entries" />
        <activity android:name=".Activities.Report" />
        <activity android:name=".Activities.UserPage" />
<activity android:name=".Activities.AdminPanel"/>
        <activity android:name=".Activities.Forget"/>
        <activity android:name=".Activities.Update"/>


        <activity
            android:name=".Activities.SignUpActivity"
            android:label="@string/title_activity_sign_in"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activities.Adduser"
            android:label="@string/title_activity_adduser"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>